import 'package:flutter/material.dart';

class BellmanDialog extends StatelessWidget {
  const BellmanDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog(
      child: Text('Hello world'),
    );
  }
}
